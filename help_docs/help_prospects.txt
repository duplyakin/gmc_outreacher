1. /prospects/sequence/stop (POST) - завершает sequence для заданных prospects 
request:
_prospects - список объектов проспектов (это то что передается когда ты в таблице выделяешь галочкой, смотри как assign сделан)

response:
{
        'code' : -1,
        'msg' : '',
        'stopped' : // Кол-во остановленных проспектов (никак не используем это в интерфейсе)
}
