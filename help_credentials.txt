1. /credentials/refresh (POST) - обнволяет credentials
request:
_credentials_id - ID credentials

response:
{
        'code' : -1,
        'msg' : 'Unknown request',
        'credentials' : //Объект обновленный
}
